/usr/bin/networkctl list --no-pager --no-legend

/lib/systemd/systemd-update-utmp runlevel

/usr/bin/savelog -q -p -n -c 5 /var/log/dmesg

/sbin/agetty -o -p -- \u --noclear --keep-baud console 115200,38400,9600 vt220

date +%Y%m%d%H%M%S

basename /usr/bin/savelog

which gzip

/bin/journalctl --boot 0 --dmesg --output short-monotonic --quiet --no-pager --no-hostname

/bin/chgrp adm /var/log/dmesg

/lib/systemd/systemd-hostnamed

groups

lesspipe

basename /usr/bin/lesspipe

dirname /usr/bin/lesspipe

dircolors -b

ls --color=auto

ls --color=auto var

ls --color=auto var/log

cat var/log/auth.log
